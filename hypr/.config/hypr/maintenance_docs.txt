You have a package called 'pkgstats' that tracks how frequently each system package is used. Use this to figure out what is only and never used, and delete it.

Also periodically run 'maintenance.sh' in hypr/scripts to clean up :)
